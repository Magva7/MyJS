/**Используя цикл, сформируйте массив list со значениями от 10 до 20. Выведите значение переменной list в консоль.**/
let list = [];
for(let i = 10; i < 21; i++){
    list.push(i);  // складываем все элементы массива
}

console.log(list);